<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/background_splash_color</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/background_splash_color</item>
        <item name="android:statusBarColor">@color/background_splash_color</item>
        <item name="android:background">@drawable/background_splash</item>
        <item name="colorPrimaryDark">@color/background_splash_color</item>
    </style>
</resources>
